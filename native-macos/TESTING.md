# macOS verification

## Sulphur response and header dragging — 2026-09-13

- The user reported an unreadable-response banner while checking Dead Man's Sulphur, stock **750**, in `Limey Whelps (PL86569)`, and reported that the panel could not be dragged normally.
- A live official Standard exchange request returned **HTTP 200** with an ID, `complexity: null`, `result: []`, and `total: 0`; response fixture: `/private/tmp/awakened-sulphur-standard-response.json`. The previous Swift decoder required a result dictionary. Replaying this actual response offline reproduced the exact banner, establishing a decoder failure for a valid empty exchange result. The Standard observation does not establish the private league's current listings.
- The decoder now accepts an empty `result` array while continuing to reject invalid result containers. Empty-array and invalid-container regressions passed with **41 TradeClient tests**. The full Swift suite passed **82 tests with 0 failures**; log: `/private/tmp/awakened-drag-exchange-tests.log`.
- The header source replaces the tiny drag grip with two native drag regions, each **84 pixels** wide, beside the centered **220-pixel** league control in the **460-pixel** panel. They use native `performDrag`, accept the first mouse click, and show an open-hand cursor. The center league control remains a click target; the body is unchanged.
- Release packaging succeeded in **41.52 seconds** (`/private/tmp/awakened-drag-exchange-package.log`), the updated app was installed, and strict code-signature verification passed. Its CDHash is **`1150f59fb6563a838e71fd18b7c693f5b6bfb8df`**.
- Computer Use visually checked the installed header: menu, centered league and X displayed correctly. Coordinate drags from **[60, 14]** to **[180, 84]**, and later from **[65, 14]** to **[145, 74]**, both failed with `noWindowsAvailable`. Those tool attempts did not verify dragging; the user acceptance result is recorded below.
- Renewing Accessibility with a reset scoped to this app and enabling its new entry opened the Mac password dialog. After the user completed it locally, Computer Use verified the system toggle ON and native Settings explicitly reporting **Accessibility enabled**, with **Ctrl+D** selected. The actual built-in browser was ready and logged in. These checks apply to the unchanged `1150f59fb6563a838e71fd18b7c693f5b6bfb8df` build; no rebuild or new tests were needed.
- **The sulphur private-league retest succeeded without the error banner.** Loaded the real history entry for Dead Man's Sulphur, displaying stock **750**, in canonical `Limey Whelps (PL86569)`, then clicked Search. The native panel showed **Matched 0**, **0 shown**, and **No matching exchange offers**. Computer Use visually verified this valid empty result with no error banner; this does not imply any offers were available.
- Restored Grattus Signet with no modifiers selected; its search completed through the same private league on the current build with **27 matching listings** and **20 loaded**. The panel was left open on the ring, with current Accessibility permission verified.
- **Physical header dragging is USER-VERIFIED successful.** On **2026-09-15**, the user replied **“good it worked”** to the request to drag the blank top bar beside the league name. This completes the remaining drag acceptance check. No rebuild or additional UI action was performed after that confirmation.
- The AppKit X implementation is unchanged, but its earlier user-confirmed physical-close result remains historical. No new physical-close or game-capture check is claimed for this build.

## Physical close-button regression — 2026-09-13

- The user again reported that physically clicking X did not close the price panel. Earlier accessibility-action and close/reopen checks did not establish that the physical pointer path worked; their historical results remain below.
- The first fix used `PricePanelCloseButton.swift`, a native `NSButton` with `acceptsFirstMouse` returning true and `mouseDownCanMoveWindow` returning false. It retained the 28×28 close symbol and accessibility action, and MainView used this button. PoeTheme also set `isMovableByWindowBackground` to false. Mouse interception by window dragging was the working hypothesis, not a proven exact cause. This attempt was later superseded after failing the user's physical-click acceptance check.
- The full SwiftUI debug compile passed in 6.02 seconds; log: `/private/tmp/awakened-close-build.log`. Unrelated network tests were not rerun. Compilation alone does not verify physical clicking.
- An isolated GUI probe containing a byte-for-byte copy of the new control was built, passed strict signature verification, and launched at a normal window level. Computer Use coordinate clicking still returned `noWindowsAvailable`, as it had for the floating production app. That probe did not verify physical mouse delivery, first-click behavior or dragging.
- Invoking the probe's new native X through its accessibility action hid the probe window. Opened OtherWindow afterward and inspected the Window menu: it listed only OtherWindow, not CloseButtonCheck. This verifies hiding without the inspection tool reopening the target window; it is not a physical-click result. The probe was quit after testing.
- The old production app at approved CDHash `31a30ae4d1a664d85f240f43ea8e702999be7380` was quit. Packaging completed successfully (release build: 9.02 seconds; log: `/private/tmp/awakened-close-package.log`), and strict signature verification passed. The installed new package has CDHash **`d4e60e695f898adbb24c8da1eaf0e36020d7c279`**.
- Launched the actual updated production app. Its main window was `main-AppWindow-1`, and the new close control exposed accessibility identifier `price-panel-close`. Pressing X through its accessibility action switched the visible window to the browser; the Window menu listed only Path of Exile Trade Browser, with the main window absent. Explicit Command+N reopened the same `main-AppWindow-1`. This verifies the installed control's hide/reopen behavior through accessibility actions, not physical clicking.
- Renewed Accessibility for only this app with a targeted reset, toggled its fresh entry ON without a password dialog, and verified **Accessibility enabled** in native Settings for the `d4e60e695f898adbb24c8da1eaf0e36020d7c279` build. This permission result predates the subsequent borderless-window source changes.
- Before the old app was quit, Flesh and Spirit showed **21 private-league matches** with **20 loaded**. Restored that saved item and its private league in the updated main window without automatically starting a search. The game was not running and was not launched. Reopening the Trade browser again showed a Cloudflare checkbox awaiting verification, so the earlier authenticated native search results remain historical; sign-in and authenticated transport were not reverified on this new build.
- The user confirmed that physically clicking X **still failed** in the installed `d4e60e695f898adbb24c8da1eaf0e36020d7c279` build. The native-button/first-mouse fix therefore failed user acceptance despite its successful accessibility-action checks. Computer Use coordinate clicking had failed in both the normal-level probe and floating app, so those tool checks did not contradict the user's report.
- The second attempt applied `pricePanelScene.windowStyle(.plain)` unconditionally and temporarily raised the minimum macOS version to **15**. A proposed macOS 14 fallback was removed because SceneBuilder did not support the conditional-availability approach. This design removed hidden-titlebar styling and the top safe-area override, used an explicit 12-point corner radius with a clear window background, and provided a separate 16-pixel native header drag grip using `performDrag`, without a header overlay gesture. It retained WindowGroup for Edit commands and window lifecycle.
- The full Swift suite passed **80 tests with 0 failures** for that plain-WindowGroup source; log: `/private/tmp/awakened-borderless-tests.log`. Packaging then succeeded (release build: **67.25 seconds**; log: `/private/tmp/awakened-borderless-package.log`), and strict signature verification passed for CDHash **`095e444720bfef58c87bf7ded5885aafd99b83b7`**. These passes did not catch the subsequent keyboard regression.
- The actual installed plain-window app exposed accessibility role **dialog**. Pasting into both the main TextEditor and Minimum Armour field timed out and left their values unchanged. Pasting into the Settings Account field worked; that temporary text was cleared. Runtime log `/private/tmp/awakened-borderless-window.log` repeatedly reported that `SwiftUI.AppKitWindow` returned **NO** from `canBecomeKeyWindow`.
- The plain-WindowGroup attempt was **rejected because of this keyboard regression**, before physical mouse verification by the user. It is not the final fix and does not establish successful physical X clicking, first-click behavior or dragging.
- The implemented replacement in `AppKitPricePanelController.swift` owns a borderless `NSWindow` whose `canBecomeKeyWindow` and `canBecomeMainWindow` explicitly return true. It preserves SwiftUI content, Settings and normal responder-based Edit menus. Escape, Command+W and close hide the reusable window, and the controller registers it again when showing it. The minimum supported macOS version is restored to **14**.
- The full Swift suite passed **80 tests with 0 failures** for this AppKit-controller source; log: `/private/tmp/awakened-appkit-tests.log`. Release packaging succeeded in **12.32 seconds** (`/private/tmp/awakened-appkit-package.log`), and strict signature verification passed. The installed package has CDHash **`041efe46590cd1835c779461b3e837a78ce49989`**.
- The installed AppKit build exposed a standard accessibility **window**, identifier `main-price-panel`, with initial focus in the main TextEditor. Pasting **“Window input check”** succeeded there, where the plain-WindowGroup build had failed. Command+Comma opened Settings, Escape dismissed it, and the Account field remained blank.
- In the actual main window, selected the Minimum Armour field with Command+A, pasted **78** over **77**, and pressed Tab; the value changed successfully. Restored the minimum to **77**, left the maximum empty, and returned Armour to **OFF**. Restored saved Flesh and Spirit and the canonical `Limey Whelps (PL86569)` without running a query.
- Renewed Accessibility for only this app with a targeted reset and enabled the new entry without a password prompt. Native Settings explicitly reported **Accessibility enabled** for the final `041efe46590cd1835c779461b3e837a78ce49989` build.
- One Computer Use coordinate click at **[437, 14]** returned success in 0.4935 seconds, but subsequent coordinate actions returned `noWindowsAvailable`. Inspecting the app automatically reopened the main window even while About was open, so the successful tool return was **not conclusive proof of hiding**. The user was then asked to physically click the updated X; the acceptance result follows.
- **Final acceptance: physical X closing is USER-VERIFIED successful.** The user replied **“Yes, it closes”** after testing the visible Flesh and Spirit panel in AppKit build **`041efe46590cd1835c779461b3e837a78ce49989`**. The app was left closed, with no further UI action or rebuild. Dragging and a fresh physical hover-and-Ctrl+D capture remain unverified for this build; the close confirmation does not establish separate first-click behavior from an inactive window.
- No new browser sign-in, live query or game-capture verification was performed for this final AppKit build. The game was running, but the agent sent no input to it. Earlier authenticated searches and physical capture remain historical evidence for their respective builds.

## Windows price-check parity — 2026-09-13

- Compared the native workflow against the checked-out Windows price-check controls, shared query builders, listing grouping, exchange flow, unidentified resolver, and market panels. [PARITY.md](PARITY.md) records the scope and source mapping.
- Final-source combined Swift suite: **80 tests passed** (15 capture, 14 JavaScriptCore bridge, 39 trade transport/results/rate-limit, 12 market/prediction). **28 Node tests passed** (15 bridge and 13 tests of the actual browser transport scripts). Logs: `/private/tmp/awakened-parity-final-tests.log` and `/private/tmp/awakened-parity-final-node-tests.log`. These include the completed browser and bulk-whisper source fixes. Network fixtures test behavior without live credentials or automatic POST retries.
- Final packaging succeeded and strict code-signature verification passed. Source and packaged JavaScript resource SHA-1 both equal `eb6c6b2abc2f4820a235a37f58ad7baa9c77fdd6`. The final executable CDHash is `31a30ae4d1a664d85f240f43ea8e702999be7380`.
- Computer Use loaded the user's real saved The Baron and verified the compact native panel visually. Its Standard search with Windows merchant defaults returned **32 matches** and loaded **20 listings**; Load more preserved the search and expanded it to **30**. These are observations at test time, not permanent market counts.
- In Search options, enabling offline sellers selected Last 2 months and disabled merchant-only; turning it off restored Any time. The Settings UI exposes Ctrl+D, Option+D, Command+D, account/seller settings, merchant/currency/stock defaults, grouping, roll range and extra delay. Prediction is visibly off by default.
- The Orb of Alteration fixture routed to the actual exchange API and returned **24 offers**: the Chaos tab displayed **6** offers and Divine displayed **18**, with corresponding unit prices, bulk ratios, stock, fulfillment counts, account states and poe.ninja market ratio. Currency switches used the already loaded result.
- The unidentified Amethyst Ring fixture showed six candidate buttons. Selecting Ming's Heart built its unidentified query and returned **6 live Standard listings**.
- Actual UI testing found two issues missed by compilation: row buttons lost their accessibility action after the combined-label modifier, and pressing Enter could restore a stale numeric value. Both were fixed before the final package; the final on-machine retest is recorded below.
- Packaged-app retest: set the Baron's leech maximum to 1 (range error), then 2 (valid). Pressing Enter retained 2 and started a successful 32-match search. The rows now expose actual accessibility buttons; clicking one opened its seller popover with price, account, status, listing date, stash and merchant fee.
- The revised bulk-row action was subsequently retested live with Orb of Alteration in Standard: **23 matches**, with **6** shown in the Chaos tab. Clicking a row opened the seller popover with account, character, status, stock and listing time. Its whisper text exposed raw placeholders. A subsequent HTTP 200 response confirmed the API's separate templates for each side of the exchange. The completed formatter uses each listed lot, preserves the API wording and offers Trade as the fallback when a template cannot be resolved. Two new regression tests passed in the 80-test suite.
- The final packaged bulk retest returned **21 Standard matches**, with **6** shown in the Chaos tab. Clicking a row opened the popover with fully filled text including **“4 Orb of Alteration for my 1 Chaos Orb”** and a **Copy whisper** control. No actual copy or send was performed.
- Invoked X and observed reopening retained `main-AppWindow-1`. Computer Use automatically reopens the app during inspection and coordinate scrolling still returns `noWindowsAvailable`, so this is not proof of a physical pointer click. Accessibility scrollbar actions worked for inspecting off-screen listings.
- The earlier approved main app's built-in browser remained at Cloudflare verification after an ordinary reload. A temporary browser-only app at `/private/tmp/poe-browser-compat.0jsl_oqk`, with `about:srcdoc` child-frame and initial-popup support, reached the actual official PoE `/login` Sign In page offering Email, Steam, Epic, Xbox and PlayStation Network. The agent did not solve a CAPTCHA. This demonstrates that WKWebView can reach that page in the diagnostic setup; it does not isolate `srcdoc` support as the cause of the different result. The final package now includes that support.
- The user subsequently completed sign-in in the diagnostic browser. Computer Use observed “Logged in as” and `/my-account`; the selected character's league label was exactly **“Limey Whelps (PL86569) League”**. At that observation the production setting still contained `LIMEY WHELPS(PL86569)`. The official Trade site selector then independently confirmed the canonical identifier **`Limey Whelps (PL86569)`**.
- In the signed-in diagnostic browser, selected that official private league and The Baron / Close Helmet through the site's autocomplete, then clicked Search. The website showed **15 results** with real listings for an item-only query with status Any. This established website account access; native transport was tested separately below. No whisper, travel or trade action was performed. The canonical **`Limey Whelps (PL86569)`** was subsequently saved in the production app through its UI. The picker heading is now **Available leagues**, avoiding an incorrect public-only label when authenticated.
- After the final packaged retest, restored The Baron and the canonical `Limey Whelps (PL86569)` in the native app, with **Use browser session ON**. Its final browser initially appeared blank; ordinary refresh reached a Cloudflare challenge. Opening the exact native query through Trade showed an actionable, unchecked **驗證您是人類** checkbox. The agent left it for the user; after the user reported sign-in complete, Computer Use verified **“Logged in as”** in the actual production browser on the correct private league.
- **Native authenticated search and fetch succeeded in the final app.** The Baron's original three enabled filters—base percentile minimum **65**, zombie leech minimum **1.61**, and minions' maximum life minimum **20**—returned **0 matching listings** without an error. Temporarily unchecked only the minion-life filter and searched again: **5 matching listings**, **5 loaded**, with five real seller rows all priced at **1 chaos**. This verifies the native authenticated POST and listing fetch through the browser transport, not merely the website search.
- Restored the minion-life checkbox to **ON**, returning to the original three selected modifiers. Results were invalidated by that filter change as expected; no subsequent search result is claimed. The canonical league remained `Limey Whelps (PL86569)` and **Use browser session remained ON**. No source or packaged-app changes were made during these authenticated checks.
- Before the final rebuild, renewed only this app's stale Accessibility entry using the user's existing authorization. The user completed the Mac password dialog; Computer Use verified the system switch was **ON** and that earlier binary displayed **“Hover an item and press ⌃D”** without an Enable banner. No credentials were read or entered by the agent.
- **Accessibility permission is verified for the final current binary.** A targeted `tccutil reset Accessibility local.awakened-poe-trade.native` succeeded; Enable Accessibility in the app recreated an OFF entry. Computer Use toggled only Awakened PoE Trade **ON** in System Settings, without a new password dialog, then used **Check Again** in native Settings and observed **Accessibility enabled**. The executable CDHash remained `31a30ae4d1a664d85f240f43ea8e702999be7380`; no further build or source change occurred.
- No new physical hover-and-Ctrl+D capture was performed during this final permission check. The user's successful live capture before the latest build remains a historical observation, not proof of a fresh capture by this binary.
- League spelling, case and suffix spacing are preserved exactly after trimming surrounding whitespace; the officially verified and successfully searched native identifier is `Limey Whelps (PL86569)`.
- Live poe.ninja data was displayed in Standard. No private-league substitute pricing, live poeprices.info prediction, feedback submission, whisper sending, purchase, or trade transaction was performed.

## Successful shortcut and private-league rejection — 2026-09-13

- The user confirmed Ctrl+D captured an item successfully. The native panel showed The Baron (Close Helmet) with its base percentile, zombie-leech and minion-life filters, confirming the combined in-game copy/parse flow after Accessibility renewal.
- Searching the selected private league `LIMEY WHELPS(PL86569)` returned HTTP 400, Invalid query. The exact same generated item payload succeeded against Standard with HTTP 200 and 34 matches in a controlled diagnostic request. A second request changing only the outdated false `foulborn_item` filter key to the current `mutated` key also returned 34 matches. Requests were spaced six seconds apart and were not retried.
- Current official filter metadata uses `mutated` for Foulborn; the old false key did not cause this particular failure. Base percentile and all emitted The Baron stat IDs exist in the live metadata. Source and packaged query output matched.
- The native client has no account-session integration. The upstream maintainer confirms that anonymous users cannot search private leagues: https://github.com/SnosMe/awakened-poe-trade/issues/1515#issuecomment-2630485062 . The Windows proxy uses its integrated browser session.
- Opened the exact failed query with Trade in Safari. Cloudflare human verification is awaiting user approval/completion, so authenticated private-league results and its canonical name remain unverified. No browser credentials were extracted, no league setting was changed, and the current signed app was not rebuilt.

## Accessibility approval investigation — 2026-09-12

- The user enabled the Awakened PoE Trade row in System Settings, but the running app continued to report Accessibility required. Computer Use verified the enabled system row and the app's denied state, including Check Again and a quit/relaunch of the exact current bundle.
- The package passes signature verification but uses an ad-hoc designated requirement tied to its executable CDHash. No valid code-signing identities were available in the accessible keychains. Changed builds therefore cannot reliably retain the previous build's privacy approval.
- The permission UI reads `AXIsProcessTrusted()` on refresh, activation, and shortcut use; it does not persist an old permission result. No further app rebuild was performed during this investigation.
- The user approved renewing this app's permission. Later targeted `tccd` logs confirmed the stored requirement used CDHash `513acedc…` while the current process/disk used `ab42ac59…`, producing “Failed to match existing code requirement.” The current process and bundle signatures match; no duplicate native installation was found.
- Toggling the old entry had retained its obsolete code requirement. After approval, a scoped `tccutil reset Accessibility local.awakened-poe-trade.native` removed only this app's stale entry. Computer Use verified its removal, invoked Enable One-Step Shortcut in the unchanged current app, and observed a newly created entry with permission off.
- Enabling that fresh entry opened macOS's password dialog. After the user completed authentication, Computer Use verified the system toggle was on and the native Settings sheet reported **Accessibility enabled**, with **Ctrl+D (Windows default)** selected. The old error message was cleared with New Price Check. No app rebuild was performed.
- The app returned focus to Path of Exile. The user subsequently confirmed the physical hover-and-Ctrl+D check succeeded; see the 2026-09-13 record above.

## League selector fix — 2026-09-12

- Replaced the titlebar league menu and the Settings text-only control with a shared picker: public league list, selected indicator, refresh/error state, and explicit Private or custom league input with Use League.
- Successful public league lists are cached across launches. The manual field accepts the exact private identifier including its `(PL12345)` suffix; submitting trims surrounding whitespace and saves the custom selection.
- Read-only live league GET requests returned HTTP 200. The trade endpoint supplied eight distinct public league IDs, including Allflame; its response matches the existing decoder, so no network endpoint change was needed.
- The packaged release rebuilt and passed signature verification. Computer Use opened the picker from both the titlebar and Settings, selected Allflame, observed the updated title, and restored Standard. The full private-name example enabled Use League; clearing it disabled the button. The example was not submitted or saved.
- No actual private-league identifier was supplied, so a live private-league trade search remains unverified. The native client has no signed-in browser session; Trade remains the browser fallback.

## Close-button fix — 2026-09-12

- Registered the actual price window instead of using `canBecomeMain` to identify it. X/Esc now hide that window directly, and reopening restores it. A genuine window close clears the reference so the scene can be created again.
- Disabled duplicate default reopen handling after the app handles the request. Before the fix, a Computer Use close/reopen changed the window ID from `main-AppWindow-4` to `main-AppWindow-6`; afterward the button action/reopen retained `main-AppWindow-1`.
- Enlarged the X hit area to 28×28 points and disabled hit testing on the background window-configuration view.
- The release build and package signature verification passed. Invoked the close button through its accessibility action in the packaged app. Direct coordinate clicks returned Computer Use `noWindowsAvailable`, and Dock activation timed out; a physical pointer click remains unverified by the tool.
- The rebuilt ad-hoc-signed app reported Accessibility disabled. The user may need to re-enable its permission for the game shortcut after rebuilding.

## Compact panel and combined shortcut revision — 2026-09-12

- The native layout now follows the Windows 460px price panel, source palette, bundled Fontin SmallCaps/Exo2 fonts, property chips, inline min/max rows and striped listing table.
- Ctrl+D replaces the prior three-key clipboard shortcut. Option+D and Command+D are selectable alternatives. While the game is focused, it copies and checks a hovered item in one action; macOS Accessibility permission is required.
- Added 15 pure capture tests covering stale clipboard text, fresh item capture, key-release/copy timeouts, cancellation, focus changes, item validation and clipboard ownership.
- Expanded network tests to 13 for optional Stock/iLvl/Quality/Gem Level listing fields and compatibility.
- All 36 Swift tests passed on this Mac: 15 capture, 8 JavaScriptCore bridge, and 13 network tests. The release app rebuilt successfully and passed strict signature verification.
- Computer Use relaunched the packaged release and visually verified the 460-point panel using the user's saved Tanu Ahi. Confirmed the original fonts, inline bounds, collapsible modifier rows, compact menus and native Settings sheet.
- Edited the life-leech minimum to 2.21 and maximum to 2: the modifier enabled automatically, an explicit range error appeared, and Search/Trade disabled. Changing the maximum to 3 restored both controls.
- Changed the shortcut to Option+D and verified the displayed status changed; restored Ctrl+D. Confirmed Command+D is also offered.
- Two live filtered Tanu Ahi searches returned HTTP 503, “Temporarily Unavailable.” Both displayed a recoverable service error; no successful live listing table was observed in this revision.
- Accessibility remains disabled pending the user's permission confirmation. Live delivery of the combined shortcut and synthetic Ctrl+C has not been verified. Computer Use's game key attempt did not establish a successful capture; unit tests do not substitute for this check.

## Initial revision — 2026-09-11

Test machine: Apple Silicon (arm64), macOS 26.6.2, Xcode Swift 6.2.4. Date: 2026-09-11.

## Automated checks

- 8 Node bridge regression tests passed: English/Traditional Chinese items, canonical query parity, rare/unique/currency parsing, grouped and numeric filters, reversed ranges, malformed input, and reduced-stat inversion.
- 19 Swift tests passed on this Mac: 8 exercising the actual JavaScriptCore resource and 11 exercising URLSession through URLProtocol fixtures.
- The full SwiftUI executable compiled in debug and release. App metadata lint and `codesign --verify --deep --strict` passed.
- Network regressions cover successful results, first-10 limits, empty results, removed listings, malformed responses, HTTP 403/429, Retry-After, cancellation, serialization, and URL encoding. These tests use fixtures; they do not impersonate live results.

## Computer Use checks

- Launched the packaged native `.app` and inspected its accessibility tree and visual layout.
- Used Load Example to parse Tabula Rasa. A real API search returned 3,516 matches in Standard and displayed the first 10 listings (observed during this run; counts/prices change).
- Used Check Copied Item with the user's actual Ctrl+C clipboard from the running game: Tanu Ahi, Wyrmscale Gauntlets, item level 82. Confirmed 225 Armour/Evasion, 11% attack speed, 145% increased defences, decimal life leech, and both multiline special modifiers.
- Enabled the life-leech filter and typed a 2.21 minimum. A maximum of 2 produced a readable validation error and disabled Search/Open Trade Site.
- Corrected that maximum to 3; Search became available and a real Tanu Ahi API search returned 605 matches in Standard, displaying 10 listings.
- Invalid clipboard text produced a recoverable parsing error instead of a stale item/search.
- Recent items showed both the real gloves and the earlier Tabula Rasa after relaunch. Opened the native Settings sheet and checked language, league, history and appearance controls.
- Closed the main window with Command–W; reopening created a new native window and preserved the shared item history.
- Relaunched the final release and verified the corrected Ctrl+C instructions and history restoration. Open Trade Site received a successful macOS browser-open completion. Safari page verification was limited by intermittent ScreenCaptureKit capture errors.

Computer Use could read the running Path of Exile window and send keys, but coordinate pointer actions returned `noWindowsAvailable`. The user therefore performed the in-game copy; the native app read that real clipboard. Full automatic in-game hover/copy and global-hotkey delivery have not been verified by this tooling. The shortcut registration status is visible in the app.
